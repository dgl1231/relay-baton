# Icons

Placeholder. Generate the real icon set before a production build:

```bash
cargo tauri icon path/to/icon.png
```

This writes `icon.png`, `icon.ico`, `icon.icns`, and the various sized PNGs
referenced by `tauri.conf.json`.
