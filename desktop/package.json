{
  "name": "relay-baton-desktop",
  "version": "1.1.3",
  "private": true,
  "description": "Desktop shell for relay-baton (Tauri sidecar over the CLI)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
